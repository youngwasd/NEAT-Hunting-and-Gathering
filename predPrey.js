class predPrey {
    static PREDLIST = new Map();
    static PREYLIST = new Map();

    static PREDPOP = 0;
    static PREYPOP = 0;

    // move colors for pred prey (maybe)
    // maybe move evolution
}